import 'dart:io';

void main() {
  stdout.write(
    'Enter your name: ',
  ); // no newline, like C++'s std::cout without endl
  //The ',' after 'Enter your name :' is trailing comma it just there for better formating of the written code
   
   // String? name = stdin.readLineSync();
  // print('Hello, $name!');
}
