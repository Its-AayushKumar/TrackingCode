#include <stdio.h>

int main()
{
    int age;
    printf("Enter your age \n:");
    scanf("%d", &age);
    if (age > 18)
    {
        printf("You are eligible for voting\n");
    }
    else
    {
        printf("You are not eligible for voting\n");
    }
    if (age = 50 ){
        printf("Half century \n");
    }

    return 0;
}