#include <stdio.h>

int main()
{
    // char str[] = {'A', 'a', 'y', 'u', 's', 'h', '\0'};

    // '\0' is the null character to tell the string ends here

    char str[] = 'Aayush'; // Here no need to put null character
    printf(str);
    return 0;
}