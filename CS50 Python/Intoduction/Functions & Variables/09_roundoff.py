x = float(input("Enter a number:"))
y = float(input("Enter a number:"))

z = round(x+y)

print(f"{z:,}")

"""The ',' is the format specifier , it tells 
python to print numbers with commas as thousand seperators. """
